## findings — carried over from other work (fold these in when you start)

### {{YYYY-MM-DD}} · found while working on {{WHAT}}
**Touches**: premise | next step | cost — one line
{{A few lines of body. Push numbers into docs/ and point at them from here.}}

<!--
  EXACT POSITION -- this block has been got wrong before, and getting it wrong
  reports ZERO pending findings, which reads as "nothing waiting".

    - the heading is `## findings`, h2, column zero. NOT inside a blockquote
    - every entry is `### YYYY-MM-DD · <what>`, h3. NOT a numbered list
    - the block ends at the next h1 or h2

  Paste this whole block into the OTHER plan, at the end. Then leave a marker on
  that plan's entry in GRAPH.md -- `⚠️ findings x2 (MM-DD)` is enough -- because
  what is not visible from the entry point does not get found.

  Do NOT raise that plan's advanced-date: the other pipeline did not move.
  Do NOT rewrite its `next`, and do NOT add sections to its body. What comes
  first is decided by the session that runs that pipeline. This block is a
  quarantine area, and that is the whole reason it exists.

  Bar for entry: does it change the other side's premise, next step, or cost?
  "Good to know" fails.

  Full rules: references/workspace.md ("## findings"), document-format.md
-->
