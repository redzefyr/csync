## {{ONE-LINE TITLE — the standing choice}} ({{YYYY-MM-DD}} · {{JUDGMENT}})

{{What was decided, in one or two sentences.}}

**Why.** {{The ground it stands on. Without this the next session reverses it.}}

<!-- Delete this comment once filled in.
  Paste as a new `##` entry at the end of notes/knowledge.md. Flat list, no
  nesting -- an entry that needs subsections is a docs/ document with a one-line
  note pointing at it.

  JUDGMENT is the value declared for `judgment` under `entry_markers` in that
  file's frontmatter, in that file's language. It says Claude concluded this by
  working: revise it without asking once its ground stops holding, and say so in
  the session report. If the user affirms it, it moves to notes/decisions.md as
  a `mandate` -- it is not re-marked here.

  The reason paragraph opens with the bold run declared as `why_marker` in that
  file's frontmatter (default "Why."). Write the GROUND -- what makes the choice
  right -- not how it was found.

  Two tests decide entry, and it needs both: "would starting work without
  knowing this make you wrong?" ("good to know" fails, and that is most of the
  bulk), and "where is the reader standing when it fires?" -- a choice settled
  inside ONE routine, type or interface, that nothing outside it can be wrong
  about, belongs in a comment at that code site instead.

  After pasting, run ~/.claude/skills/csync/scripts/csync-ledger.sh gauges.

  Rules: references/workspace.md ("notes/")
-->
