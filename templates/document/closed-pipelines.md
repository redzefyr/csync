---
csync: archive/1
---

# Closed pipelines

One line each, newest first. Appended as a pipeline closes; **nothing here is
revised afterwards.**

- ~~[[slug]]~~ closed YYYYMMDD `<commit>` — one-line conclusion

<!-- Delete this comment once the first entry is in.
  Lives at `docs/closed-pipelines.md` -- the one document allowed at the root of
  `docs/`, because it belongs to neither tier: `design/` is revised when the code
  changes, `archive/` is the judgment of a day, and this is an index that is only
  ever appended to. The kind is `archive/1` for the lifetime it states -- left as
  written -- not for the directory.

  `GRAPH.md`'s `## closed pipelines` section names this file and carries no
  entries of its own, so this is where a closed slug resolves.

  ONE LINE, and it states the conclusion -- not how the work went. Whatever the
  pipeline established has already gone where the closing table sent it: notes/,
  a comment at a code site, docs/archive/, docs/design/. This line records that
  the pipeline existed, ended, and what it settled.

  Newest first, so the file reads from the top and the oldest entries are the
  ones nobody scrolls to. Nothing is deleted from here.

  Rules: references/workspace.md ("Closing a pipeline")
-->
