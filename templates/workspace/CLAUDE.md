# {{WS}} — {{NAME}}

{{DESCRIPTION}}

Claude's workspace for this project, synced to the `prj/{{NAME}}` branch of the
csync repo. How everything in here is organised is the csync skill's
`~/.claude/skills/csync/references/workspace.md`; what is here is listed by
`~/.claude/skills/csync/scripts/csync-ledger.sh`, run from the project root.

<!-- Facts about THIS project only; no copy of the rules. This file is copied once
  at init and never updated, and Claude Code loads it whenever a file under this
  directory is Read -- so a rule here goes stale everywhere at once, and every
  line is paid for by every session that reads notes/.
  Rules: ~/.claude/skills/csync/references/setup.md
-->
