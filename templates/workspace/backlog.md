---
csync: backlog/1
gauge: 50/
---

# Backlog — work with no plan yet

Items are `- YYYYMMDD <what>`, dated when listed; nothing else here starts `- `.
Done → distribute, delete the line · needs a plan → propose, wait · stale →
propose dropping, wait. After writing, run
`~/.claude/skills/csync/scripts/csync-ledger.sh gauges`; past the max, propose —
never promote or drop alone. Rules:
`~/.claude/skills/csync/references/workspace.md` ("The backlog").

